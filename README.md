# Voice-Control-Assistant
Accepts voice input from the user, Processes commands using conditional logic, Executes basic tasks based on recognized speech, It uses the voice command and performs the task
